I am going to develop a requirement for the sql visualizer of a project. 
1.input a folder, automatically display the hierarchical folder file tree in a sub panel. There should be sql script and other types of files. Only sql script is to be analyzed and visualized by this tool, other types of files should be gray and can not be clicked. In the same time, the scrips, tables, and table fields are extracted and to be used as indexes in the following user interaction. The scripts and folders can be selected or unselected. When selected all the visualization will be performed over the selected scripts.
2.There should be a filter panel to select target field of a certain table. User can input fields first or the tables first. When inputting field first, field name can be automatically completed by the indexes extracted in 1. And then tables containing this field can be selected in the drop down list which is also generated by the indexes in 1. Certainly user can input tables first, table name can also be automatically completed, and then field names can be selected by drop down list. This is to improve input efficiency ASAP.  After table and field is confirmed. The search button is clicked, and the view of this query should be generated in 3.
3.There should be a view management tree. The first level level is the view of all scripts related to the selected table and field in 2. The second level is the view of every script on the data flow related to the selected table and field. This management tree should contain every search generated by 2, and each of them could be removed by user. When one view is activated, there should be a panel to show the data flow graph. 
4. There are two kind of data flow graph. The first level and the second level, which have been referred in 3. The first level contains the following types of nodes: scripts. All the scripts related to the data flow on the selected table and field should be visualized in the first level graph. They should form a data flow as a graph. The second level contains the following types of nodes: tables and fields. All the related tables and fields involved in the data flow to selected table and field should be visualized in this graph. They should form data flow as graph for the querying table and field.
5. When click the node (script) on the first level graph, it will open the second level graph on the clicked script. 
6. There should also a panel to show the SQL for the second level graph. The data flow related parts of the script should be high lighten. If the script is too long, it should be scrolled. There should be a export button to export data flow related SQL of the second level graph into an executable script. There should be a configuration file for sql export to be uploaded. If user doesn't upload any configuration it should use default configuration.
7. The graph should try to reuse existing code.

---

### Amendment (2026-08-12) — L1 shows the queried FIELD's flow; upstream/downstream query direction

§4 amended: the first-level graph shows the **data flow of the selected field** — the
same semantic as the second-level graph — at cross-script scale: script nodes + the
tables between scripts that carry the flow (no fields, no intra-script detail). Scripts
and tables that only read or write the selected TABLE (without the selected field's
flow) are not included.

§2 amended: the query panel gains a direction setting — **upstream** (the writing data
flow: fields writing the selected field; **default**) / **downstream** (the reading data
flow: fields reading it). The first-level graph renders the flow in the chosen
direction, and the second-level graph follows it automatically (it is the zoom-in of
the first level).
